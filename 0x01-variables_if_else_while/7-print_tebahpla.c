#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	char c;
	for (c = 'z'; c >= 'a' ; c--)
	{
	c = i + 'a';
	putchar(c);
	}
	putchar('\n');
	return (0);
}
