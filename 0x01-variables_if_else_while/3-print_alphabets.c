#include <stdio.h>
/**
 * main - Enter point
 * Description: prints the alphabet in lowercase, and then in uppercase
 * Return: always 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}
	C = 'A'
	while (C <= 'Z')
	{
		putchar(C);
		C++;
	}
	putchar("\n");
	return (0);
}
