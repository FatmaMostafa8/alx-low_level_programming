#include <stdio.h>
/**
 * main - Enter point
 * Description: prints the alphabet in lowercase
 * Return: always 0
 */
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		putchar(n);
		n++;
	}
	putchar("\n");
	return (0);
}
